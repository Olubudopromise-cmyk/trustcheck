# Frontend — URL the browser uses to reach the API
NEXT_PUBLIC_API_URL=http://localhost:8080

# Backend — CORS origin allowed to call the API (defaults to http://localhost:3000)
ALLOWED_ORIGIN=http://localhost:3000
